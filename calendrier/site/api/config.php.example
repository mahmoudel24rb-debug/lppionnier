<?php
/**
 * Configuration de l'API : copier ce fichier en `config.php` (jamais commité).
 * Générer le hash du mot de passe admin :
 *   php -r "echo password_hash('LE_MOT_DE_PASSE', PASSWORD_DEFAULT), PHP_EOL;"
 */
define('PNR_ADMIN_HASH', '');

/**
 * Mots de passe des responsables de section (un hash par section, mêmes clés
 * que PNR_SECTIONS). Laisser une valeur vide désactive la section.
 * Générer chaque hash de la même façon :
 *   php -r "echo password_hash('LE_MOT_DE_PASSE', PASSWORD_DEFAULT), PHP_EOL;"
 */
define('PNR_SECTION_HASHES', [
    'footus'    => '',
    'flag'      => '',
    'footus_jr' => '',
    'flag_jr'   => '',
    'ecole'     => '',
    'club'      => '',
]);
